<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Caleb Yoon | Marketing & Operations</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta
    name="description"
    content="Portfolio of Caleb Yoon – data-driven marketing and operations leader with experience at Amazon, Scooter Braun Projects/Hybe America, and UC Irvine."
  />
  <style>
    :root {
      --bg: #0b0c10;
      --bg-alt: #10131a;
      --card: #151820;
      --accent: #4fd1c5;
      --accent-soft: rgba(79, 209, 197, 0.08);
      --text: #f7fafc;
      --muted: #a0aec0;
      --border: #2d3748;
      --font-sans: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
        sans-serif;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: var(--font-sans);
      background: radial-gradient(circle at top, #1a202c 0, #0b0c10 45%, #050609 100%);
      color: var(--text);
      line-height: 1.6;
      -webkit-font-smoothing: antialiased;
    }

    a {
      color: inherit;
      text-decoration: none;
    }

    img {
      max-width: 100%;
      display: block;
    }

    .page {
      max-width: 1120px;
      margin: 0 auto;
      padding: 24px 16px 64px;
    }

    header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 16px;
      padding: 12px 0 24px;
    }

    .logo-text {
      font-weight: 700;
      letter-spacing: 0.14em;
      font-size: 0.8rem;
      text-transform: uppercase;
      color: var(--muted);
    }

    nav {
      display: flex;
      gap: 18px;
      font-size: 0.9rem;
    }

    nav a {
      color: var(--muted);
      padding: 4px 0;
      border-bottom: 2px solid transparent;
      transition: color 0.2s ease, border-color 0.2s ease;
    }

    nav a:hover {
      color: var(--accent);
      border-color: var(--accent-soft);
    }

    .btn-outline {
      border-radius: 999px;
      border: 1px solid var(--border);
      padding: 6px 14px;
      font-size: 0.85rem;
      color: var(--muted);
      background: transparent;
      cursor: pointer;
      transition: border-color 0.2s ease, background 0.2s ease, color 0.2s ease;
    }

    .btn-outline:hover {
      border-color: var(--accent);
      background: var(--accent-soft);
      color: var(--text);
    }

    /* Hero */
    .hero {
      display: grid;
      grid-template-columns: minmax(0, 2.2fr) minmax(0, 1.3fr);
      gap: 32px;
      padding: 32px 0 40px;
      border-bottom: 1px solid rgba(255, 255, 255, 0.04);
    }

    .hero-badge {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      font-size: 0.78rem;
      padding: 4px 10px;
      border-radius: 999px;
      background: rgba(148, 163, 184, 0.14);
      color: var(--muted);
      margin-bottom: 16px;
    }

    .hero-badge-dot {
      width: 8px;
      height: 8px;
      border-radius: 999px;
      background: var(--accent);
      box-shadow: 0 0 0 4px rgba(79, 209, 197, 0.35);
    }

    .hero-title {
      font-size: clamp(2.0rem, 3.3vw, 2.8rem);
      font-weight: 700;
      letter-spacing: 0.03em;
      margin-bottom: 12px;
    }

    .hero-title span {
      color: var(--accent);
    }

    .hero-subtitle {
      font-size: 0.98rem;
      color: var(--muted);
      max-width: 520px;
      margin-bottom: 18px;
    }

    .hero-meta {
      display: flex;
      flex-wrap: wrap;
      gap: 10px 18px;
      font-size: 0.82rem;
      color: var(--muted);
      margin-bottom: 20px;
    }

    .hero-meta span {
      display: inline-flex;
      align-items: center;
      gap: 6px;
    }

    .hero-meta-dot {
      width: 6px;
      height: 6px;
      border-radius: 999px;
      background: rgba(160, 174, 192, 0.7);
    }

    .hero-actions {
      display: flex;
      flex-wrap: wrap;
      gap: 12px;
      margin-top: 6px;
    }

    .btn-primary {
      border-radius: 999px;
      border: none;
      padding: 9px 18px;
      font-size: 0.9rem;
      font-weight: 500;
      background: linear-gradient(135deg, var(--accent), #63b3ed);
      color: #0b0c10;
      cursor: pointer;
      box-shadow: 0 10px 30px rgba(99, 179, 237, 0.3);
      transition: transform 0.15s ease, box-shadow 0.15s ease;
    }

    .btn-primary:hover {
      transform: translateY(-1px);
      box-shadow: 0 14px 38px rgba(99, 179, 237, 0.45);
    }

    .hero-secondary-link {
      font-size: 0.9rem;
      color: var(--muted);
      display: flex;
      align-items: center;
      gap: 6px;
    }

    .hero-secondary-link span {
      font-size: 1.1rem;
    }

    .hero-card {
      border-radius: 20px;
      background: radial-gradient(circle at top left, rgba(79, 209, 197, 0.16), transparent 55%),
        linear-gradient(145deg, #111827, #020617);
      padding: 18px 18px 14px;
      border: 1px solid rgba(148, 163, 184, 0.35);
      box-shadow: 0 22px 70px rgba(0, 0, 0, 0.5);
    }

    .hero-card-title {
      font-size: 0.78rem;
      color: var(--muted);
      text-transform: uppercase;
      letter-spacing: 0.13em;
      margin-bottom: 12px;
    }

    .hero-name {
      font-size: 1.3rem;
      font-weight: 600;
      margin-bottom: 2px;
    }

    .hero-role {
      font-size: 0.9rem;
      color: var(--muted);
      margin-bottom: 12px;
    }

    .hero-stat-row {
      display: flex;
      justify-content: space-between;
      gap: 10px;
      margin-bottom: 12px;
    }

    .hero-stat {
      flex: 1;
      padding: 10px;
      border-radius: 14px;
      background: rgba(15, 23, 42, 0.85);
      border: 1px solid rgba(148, 163, 184, 0.3);
      font-size: 0.8rem;
    }

    .hero-stat strong {
      display: block;
      font-size: 1rem;
      margin-bottom: 2px;
    }

    .hero-chip-row {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      margin-top: 8px;
    }

    .chip {
      font-size: 0.75rem;
      padding: 4px 9px;
      border-radius: 999px;
      border: 1px solid rgba(148, 163, 184, 0.25);
      background: rgba(15, 23, 42, 0.8);
      color: var(--muted);
    }

    /* Sections */
    section {
      padding: 40px 0 8px;
      border-bottom: 1px solid rgba(255, 255, 255, 0.04);
    }

    .section-header {
      display: flex;
      align-items: baseline;
      justify-content: space-between;
      gap: 8px;
      margin-bottom: 18px;
    }

    .section-title {
      font-size: 1.2rem;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      color: #e2e8f0;
    }

    .section-subtitle {
      font-size: 0.85rem;
      color: var(--muted);
    }

    /* Experience cards */
    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
      gap: 18px;
    }

    .card {
      border-radius: 16px;
      background: var(--bg-alt);
      border: 1px solid var(--border);
      padding: 16px 18px;
    }

    .card-heading {
      display: flex;
      justify-content: space-between;
      gap: 12px;
      margin-bottom: 4px;
    }

    .card-role {
      font-weight: 600;
      font-size: 0.98rem;
    }

    .card-company {
      font-size: 0.86rem;
      color: var(--muted);
    }

    .card-meta {
      font-size: 0.8rem;
      color: var(--muted);
      margin-bottom: 8px;
    }

    .card ul {
      list-style: none;
      padding-left: 0;
      font-size: 0.86rem;
      colo
